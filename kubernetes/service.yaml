apiVersion: v1
kind: Service
metadata:
  name: nft-preview-card-component-main
  namespace: default
spec:
  selector:
    app: nft-preview-card-component-main-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
